serve and build continuously (dev mode):
$ npm run start

lint + build (dev mode):
$ npm run debug

lint + build (production mode):
$ npm run build

lint:
$ npx eslint client.ts

compile:
$ tsc client.ts

combo (using a tsconfig.json):
$ npx eslint *.ts && tsc

